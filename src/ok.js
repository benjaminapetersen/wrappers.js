'use strict';

(function (root, undef) {

    // the thing that will eventually be exported
    var __my__object__name__ = {};

    // assuming a global first.  not ideal
    this.__my__object__name__ = __my__object__name__;
    if (typeof module !== 'undefined' && module.exports) {
            module.exports = __my__object__name__;
    }
}).call(this);
